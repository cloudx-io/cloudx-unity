<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application android:networkSecurityConfig="@xml/network_security_config">
    <activity android:name="com.unity3d.player.UnityPlayerActivity" android:configChanges="fontScale|keyboard|keyboardHidden|locale|mnc|mcc|navigation|orientation|screenLayout|screenSize|smallestScreenSize|uiMode|touchscreen" android:label="@string/app_name">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <!-- Google Mobile Ads requires an application ID at process start or the
         MobileAdsInitProvider throws. This is Google's public TEST app id;
         the demo app only ever serves test AdMob inventory. -->
    <meta-data android:name="com.google.android.gms.ads.APPLICATION_ID" android:value="ca-app-pub-3940256099942544~3347511713" />
  </application>
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <!-- Required from target SDK 33 (this project targets 36) to read the advertising ID;
       without it the ID reads back as all zeros and no dashboard test-device entry can
       ever match. play-services-ads-identifier declares it too, so this changes nothing
       about the merged manifest - it is here so a publisher copying this demo sees the
       requirement instead of inheriting it by accident. -->
  <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
</manifest>
